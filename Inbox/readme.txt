TODO:
intégrer le ModelsManager dans le DeskController
ajouter des méthodes dans le ModelsManager pour utiliser les readers 
Changer la méthode pour déplacer un message (voir dans le model aussi pour comment conserver le chemin d'avant jusque;au deplacement sur le server)
